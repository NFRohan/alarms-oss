# Keep rules should stay minimal. The current native bridge and JSON mapping are
# referenced directly rather than through reflection, so the default Android and
# Flutter rules are intentionally doing most of the work here.

# Add targeted keep rules only when a dependency or feature proves it needs them.
